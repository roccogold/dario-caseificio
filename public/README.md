# Public Assets

This directory contains static assets served directly by the web server.

## 📁 Directory Structure

- **`icons/`** - Logo files, favicons, and SVG icons
- **`fonts/`** - Custom font files
- **`manifest.json`** - PWA manifest file
- **`sw.js`** - Service worker for PWA functionality
- **`robots.txt`** - Search engine crawler instructions

## 🎨 Assets

### Icons
- `frog-logo.svg` - Main application logo
- `dario-logo.svg` - Alternative logo
- `favicon.ico` - Browser favicon
- `placeholder.svg` - Placeholder image

### PWA Files
- `manifest.json` - Web app manifest for PWA installation
- `sw.js` - Service worker for offline functionality

## 📝 Notes

- All files in this directory are served at the root URL (e.g., `/frog-logo.svg`)
- Images should be optimized for web use
- SVG files are preferred for logos and icons
