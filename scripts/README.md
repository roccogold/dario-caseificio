# Scripts

This directory contains utility scripts for development and deployment.

## 📁 Available Scripts

### Development
- **`START_DEV_SERVER.sh`** - Start the development server with proper Node.js environment setup

### Deployment
- **`deploy.sh`** - Deployment script for production

## 🚀 Usage

### Start Development Server

```bash
./scripts/START_DEV_SERVER.sh
```

Or make it executable and run:

```bash
chmod +x scripts/START_DEV_SERVER.sh
./scripts/START_DEV_SERVER.sh
```

### Deploy to Production

```bash
./scripts/deploy.sh
```

Or:

```bash
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```

## 📝 Notes

- Make sure scripts are executable: `chmod +x scripts/*.sh`
- Scripts assume you have Node.js and npm installed
- Check individual script files for specific requirements
